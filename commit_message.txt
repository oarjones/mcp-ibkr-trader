feat: Implement final module adjustments

This commit implements the following final adjustments for the module:

- **Teardown completo (`tws_client.py`):**
    - Added `_active_realtime_req_ids` list to `TWSClient` to store active real-time subscription `reqId`s.
    - Modified `disconnect()` to include comments for canceling real-time data subscriptions.

- **Resuscripción tras reconnect (`tws_client.py`):**
    - Added a comment in `connect_and_run` to remind about re-subscribing to active subscriptions after reconnect.

- **Tests del error-map (`tests/test_enhancements.py`):**
    - Added a unit test `test_error_map` to verify `IBKR_ERROR_MAP` functionality.

- **Límites de concurrencia (`tws_client.py`):**
    - Added a comment in `TWSClient` class to document the maximum number of simultaneous `reqHistoricalData` allowed.

- **Tipos de salida (`ibkr_adapter/adapter.py`):**
    - Ensured `df.dtypes` are numeric (`float64`/`int64`) and `ts` is `tz-naive` or `UTC` consistent in `get_bars`.

- **`what_to_show` (`ibkr_adapter/adapter.py`):**
    - Exposed `what_to_show` as a parameter in `get_bars(...)` and propagated it to `self.client.get_historical_data`.
